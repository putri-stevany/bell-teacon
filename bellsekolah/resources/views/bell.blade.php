{{-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bel Sekolah</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body>

    <div class="container">
        <h1>Bel Sekolah</h1>

        @if(count($bells) > 0)
            <ul class="bell-list">
                @foreach($bells as $bell)
                    <li>
                        <span class="bell-name">{{ $bell->name }}</span>
                        <span class="bell-time">{{ $bell->ring_time }}</span>
                    </li>
                @endforeach
            </ul>
        @else
            <p>Tidak ada data bel sekolah.</p>
        @endif
    </div>

    <script src="{{ asset('js/scripts.js') }}"></script>
</body>
</html> --}}
